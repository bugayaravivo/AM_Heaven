<h2>検索結果</h2>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.name, user_path(user) %></li>
    <% end %>
  </ul>
<% else %>
  <p>該当するユーザーは見つかりませんでした。</p>
<% end %>